"""Offline evaluation harness for the Noor-AI RAG pipeline.

Self-contained package (no imports from `src` app code except where a task
explicitly wires production adapters). Runnable from `backend/` via
`python -m evals ...`.
"""
