# Example Eval_Config for `python -m evals run --config evals/config.yaml`.
# All four of model_id, retrieval_top_k, prompt_version, and judge_model_id
# are required; dataset_path and results_dir fall back to package defaults.
model_id: us.anthropic.claude-haiku-4-5-20251001-v1:0
retrieval_top_k: 5
prompt_version: v1
judge_model_id: us.amazon.nova-pro-v1:0   # different family than model_id (Req 8.5)
dataset_path: data/golden_dataset.jsonl
results_dir: results
