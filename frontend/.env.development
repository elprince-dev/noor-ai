# Local dev only (loaded by `next dev`, NOT by `next build`).
# In production the value is unset, so the app falls back to "/api",
# which CloudFront routes to API Gateway.
NEXT_PUBLIC_API_URL=http://localhost:8000
